Use the following command on your terminal to run the solver on all the instances.

$ chmod +x ./run.sh && ./run.sh

The output will be saved in the output folder.

You can update the run limit in the run.sh file, denoted by the option "--time-limit 600". The option is in seconds.
Thus, 600 means 10 minutes whereas 60 would mean 1 minute and so on.